# Copia este archivo como `key.properties` (NO commits) y rellena con tus valores.
# Generar el keystore con:
#   keytool -genkey -v -keystore ~/matute-release.jks \
#     -keyalg RSA -keysize 2048 -validity 10000 -alias matute
storePassword=CAMBIAR
keyPassword=CAMBIAR
keyAlias=matute
storeFile=/Users/TU_USUARIO/matute-release.jks
